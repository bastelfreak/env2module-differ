# frozen_string_literal: true

source ENV['GEM_SOURCE'] || 'https://rubygems.org'

gem 'markdown-tables'
gem 'pl-puppetdb-ruby'
gem 'rake'

group :development do
  gem 'pry'
  gem 'rubocop'
end

group :r10k do
  gem 'r10k'
end
